# Config file for automatic testing at travis-ci.org
os: ubuntu

osx_image: focal fossa

language: python

install: "pip install -r requirements.txt"

script:
  - python test.py
  
after_success:
  - codecov
